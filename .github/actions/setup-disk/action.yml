name: Setup Disk

description: Setup Disk

author: "Grinch27"

inputs:
  path_build:
    description: Mount point to use
    required: true
    default: ""
  root_num:
    description: Reserved GiB on /
    required: false
    default: "4"
  mnt_num:
    description: Reserved GiB on /mnt
    required: false
    default: "1"

runs:
  using: composite
  steps:
    - name: Check in Docker
      id: check-indocker
      shell: bash
      working-directory: /
      run: |
        set -euo pipefail

        # 识别当前是否运行在 Docker 容器内
        echo -e "Current working directory: $(pwd)"
        if [ -f /.dockerenv ]; then
          echo "in_container=true" | tee -a "${GITHUB_OUTPUT}"
        else
          echo "in_container=false" | tee -a "${GITHUB_OUTPUT}"
        fi

    - name: Setup Disk
      id: setup-disk
      if: steps.check-indocker.outputs.in_container == 'false'
      shell: bash
      env:
        path_build: ${{ inputs.path_build }}
        root_num: ${{ inputs.root_num }}
        mnt_num: ${{ inputs.mnt_num }}
      working-directory: /
      run: |
        # 输出当前磁盘状态
        echo "Current working directory: $(pwd)"
        df -Th

        # 读取 /mnt 与 / 的可用空间
        mnt_available=$(df -B1 /mnt | tail -1 | awk '{print $4}')
        root_available=$(df -B1 / | tail -1 | awk '{print $4}')
        echo "Raw available space in /mnt (bytes): ${mnt_available}"
        echo "Raw available space in / (bytes): ${root_available}"

        # 扣除预留空间
        gb2bytes=$((1024 * 1024 * 1024))
        mnt_size=$((${mnt_available} - ${{ env.mnt_num }} * ${gb2bytes}))
        root_size=$((${root_available} - ${{ env.root_num }} * ${gb2bytes}))
        echo "Available space in /mnt (bytes): ${mnt_size}"
        echo "Available space in / (bytes): ${root_size}"

        # 创建回环映像文件
        sudo truncate -s "${mnt_size}" /mnt/mnt.img
        sudo truncate -s "${root_size}" /root.img

        # 绑定 loop 设备
        LOOP_MNT=$(sudo losetup --find --show /mnt/mnt.img)
        LOOP_ROOT=$(sudo losetup --find --show /root.img)
        echo "Assigned loop device for /mnt/mnt.img: ${LOOP_MNT}"
        echo "Assigned loop device for /root.img: ${LOOP_ROOT}"

        # 初始化物理卷并创建卷组
        sudo pvcreate "${LOOP_MNT}"
        sudo pvcreate "${LOOP_ROOT}"
        sudo vgcreate github "${LOOP_MNT}" "${LOOP_ROOT}"

        # 创建逻辑卷并格式化
        sudo lvcreate -n runner -l 100%FREE github
        sudo mkfs.xfs /dev/github/runner

        # 挂载逻辑卷并修正权限
        sudo mkdir -p "${path_build}"
        sudo mount /dev/github/runner "${path_build}"
        sudo chown -R $(whoami):$(whoami) "${path_build}"

        df -Th

    - name: Setup Disk for Docker
      id: setup-disk-docker
      if: steps.check-indocker.outputs.in_container == 'true'
      continue-on-error: true
      shell: bash
      env:
        path_build: ${{ inputs.path_build }}
        root_num: ${{ inputs.root_num }}
        mnt_num: ${{ inputs.mnt_num }}
      working-directory: /
      run: |
        set -euo pipefail

        # 输出当前磁盘状态
        echo -e "Current working directory: $(pwd)"
        df -Th

        # 统一 root 执行入口
        run_root() {
          if command -v sudo >/dev/null 2>&1; then
            sudo "$@"
          else
            "$@"
          fi
        }

        # 补齐扩盘所需依赖
        ensure_disk_tools() {
          local required_cmds missing_cmds cmd

          required_cmds=(df losetup mount mountpoint pvcreate vgcreate lvcreate truncate)
          missing_cmds=()
          for cmd in "${required_cmds[@]}"; do
            if ! command -v "${cmd}" >/dev/null 2>&1; then
              missing_cmds+=("${cmd}")
            fi
          done

          if ! command -v mkfs.xfs >/dev/null 2>&1 && ! command -v mkfs.ext4 >/dev/null 2>&1; then
            missing_cmds+=("mkfs")
          fi

          if [ "${#missing_cmds[@]}" -eq 0 ]; then
            return 0
          fi

          if ! command -v apt-get >/dev/null 2>&1; then
            echo "apt-get not found, but required disk tools are missing: ${missing_cmds[*]}"
            return 1
          fi

          run_root env DEBIAN_FRONTEND=noninteractive apt-get -yqq update
          run_root env DEBIAN_FRONTEND=noninteractive apt-get -yqq install lvm2 util-linux e2fsprogs xfsprogs
        }

        # 读取指定目录的剩余字节数
        get_available_bytes() {
          local mount_path="$1"
          if [ -n "${mount_path}" ] && [ -d "${mount_path}" ]; then
            df -B1 "${mount_path}" | awk 'END {print $4}'
          else
            echo 0
          fi
        }

        # 标识目录所在文件系统
        get_filesystem_key() {
          local mount_path="$1"
          if [ -n "${mount_path}" ] && [ -d "${mount_path}" ]; then
            df -P "${mount_path}" | awk 'END {print $1":"$6}'
          else
            echo ""
          fi
        }

        # 尝试补齐 loop 设备节点
        ensure_loop_devices() {
          local index

          if command -v modprobe >/dev/null 2>&1; then
            run_root modprobe loop >/dev/null 2>&1 || true
          fi
        # 计算可用空间并登记映像文件

          if command -v mknod >/dev/null 2>&1; then
            if [ ! -e /dev/loop-control ]; then
              run_root mknod -m 660 /dev/loop-control c 10 237 >/dev/null 2>&1 || true
            fi
            for index in $(seq 0 7); do
              if [ ! -b "/dev/loop${index}" ]; then
                run_root mknod -m 660 "/dev/loop${index}" b 7 "${index}" >/dev/null 2>&1 || true
              fi
            done
          fi
        }

        add_backing_file() {
          local label="$1"
          local mount_path="$2"
          local file_path="$3"
          local reserve_gib="$4"
          local raw_available reserve_bytes usable_bytes

          raw_available=$(get_available_bytes "${mount_path}")
          reserve_bytes=$((reserve_gib * 1024 * 1024 * 1024))
          usable_bytes=$((raw_available - reserve_bytes))

          echo "Raw available space in ${label} (bytes): ${raw_available}"
          echo "Usable space in ${label} after reserve (bytes): ${usable_bytes}"

          if [ "${usable_bytes}" -lt "${min_file_bytes}" ]; then
            echo "Skip ${label}: usable space ${usable_bytes} is below minimum ${min_file_bytes}"
            return 0
          fi

          file_paths+=("${file_path}")
          file_sizes+=("${usable_bytes}")
        }

        # 校验关键输入
        if [ -z "${path_build}" ]; then
          echo "path_build is required"
          exit 1
        fi

        # 安装依赖并准备 loop 节点
        ensure_disk_tools
        ensure_loop_devices

        # 已挂载时直接复用目标目录
        run_root mkdir -p "${path_build}"
        if mountpoint -q "${path_build}"; then
          echo "${path_build} is already mounted"
          run_root chown -R "$(whoami):$(whoami)" "${path_build}"
          df -Th "${path_build}"
          exit 0
        fi

        # 检查 loop 设备是否可用
        if ! run_root losetup -f >/dev/null 2>&1; then
          echo "No loop device available for setup-disk-docker"
          exit 1
        fi

        # 初始化本次扩盘的资源名与状态
        vg_suffix=$(printf '%s_%s' "${GITHUB_RUN_ID:-local}" "${GITHUB_RUN_ATTEMPT:-0}" | tr -cs '[:alnum:]_' '_')
        vg_name="github_${vg_suffix}"
        lv_name="runner"
        lv_path="/dev/${vg_name}/${lv_name}"
        min_file_bytes=$((1024 * 1024 * 1024))
        root_fs_key=$(get_filesystem_key "/")
        mnt_fs_key=$(get_filesystem_key "/mnt")
        loop_devices=()
        file_paths=()
        file_sizes=()

        # 失败时清理已创建的挂载与 LVM 资源
        cleanup_on_error() {
          local exit_code="$?"
          local loop_device file_path

          set +e
          if mountpoint -q "${path_build}"; then
            run_root umount "${path_build}"
          fi
          if run_root lvdisplay "${lv_path}" >/dev/null 2>&1; then
            run_root lvremove -fy "${lv_path}"
          fi
          if run_root vgdisplay "${vg_name}" >/dev/null 2>&1; then
            run_root vgremove -fy "${vg_name}"
          fi
          for loop_device in "${loop_devices[@]}"; do
            if run_root pvs "${loop_device}" >/dev/null 2>&1; then
              run_root pvremove -ffy "${loop_device}"
            fi
            run_root losetup -d "${loop_device}" >/dev/null 2>&1 || true
          done
          for file_path in "${file_paths[@]}"; do
            run_root rm -f "${file_path}"
          done
          exit "${exit_code}"
        }
        trap cleanup_on_error ERR

        # 同盘时跳过 /mnt，避免重复计入空间
        if [ -n "${mnt_fs_key}" ] && [ "${mnt_fs_key}" = "${root_fs_key}" ]; then
          echo "Skip /mnt: shares the same filesystem with /"
        else
          add_backing_file "/mnt" "/mnt" "/mnt/${vg_name}_mnt.img" "${mnt_num}"
        fi
        add_backing_file "/" "/" "/${vg_name}_root.img" "${root_num}"

        # 无可用空间时直接失败
        if [ "${#file_paths[@]}" -eq 0 ]; then
          echo "No usable space available for setup-disk-docker after applying reserved GiB"
          exit 1
        fi

        # 创建映像文件并初始化物理卷
        for idx in "${!file_paths[@]}"; do
          file_path="${file_paths[$idx]}"
          file_size="${file_sizes[$idx]}"

          run_root mkdir -p "$(dirname "${file_path}")"
          run_root rm -f "${file_path}"
          run_root truncate -s "${file_size}" "${file_path}"

          loop_device=$(run_root losetup --find --show "${file_path}")
          loop_devices+=("${loop_device}")
          echo "Assigned loop device for ${file_path}: ${loop_device}"

          run_root pvcreate -ff -y "${loop_device}"
        done

        # 创建卷组与逻辑卷
        run_root vgcreate "${vg_name}" "${loop_devices[@]}"
        run_root lvcreate -n "${lv_name}" -l 100%FREE "${vg_name}"

        # 优先使用 xfs，失败时退回 ext4
        if command -v mkfs.xfs >/dev/null 2>&1; then
          run_root mkfs.xfs -f "${lv_path}"
        else
          run_root mkfs.ext4 -F "${lv_path}"
        fi

        # 挂载逻辑卷并修正权限
        run_root mount "${lv_path}" "${path_build}"
        run_root chown -R "$(whoami):$(whoami)" "${path_build}"

        df -Th

    - name: (Fix) Setup Disk
      id: fix-setup-disk
      if: steps.check-indocker.outputs.in_container == 'true' && steps.setup-disk-docker.outcome == 'failure'
      shell: bash
      env:
        path_build: ${{ inputs.path_build }}
      working-directory: /
      run: |
        set -euo pipefail

        # Docker 扩盘失败时回退到目录模式
        echo -e "Current working directory: $(pwd)"
        sudo mkdir -p "${path_build}"
        sudo chown -R "$(whoami):$(whoami)" "${path_build}"
